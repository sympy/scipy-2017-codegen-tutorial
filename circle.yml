machine:
  environment:
    PATH: "$HOME/miniconda/bin:${PATH}"

dependencies:
  pre:
    - ./bin/circleci_dependencies.sh
  cache_directories:
    - "~/miniconda"

test:
  override:
    - ./bin/render_notebooks.sh
